{
  "name": "sqlite-mcp",
  "version": "0.2.1",
  "description": "MCP stdio server for SQLite — open / query / execute / schema inspection via better-sqlite3 (synchronous, prebuilt binaries)",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "sqlite-mcp": "dist/index.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.23.8",
    "better-sqlite3": "^11.3.0",
    "csv-parse": "^5.5.6",
    "csv-stringify": "^6.5.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/better-sqlite3": "^7.6.11",
    "typescript": "^5.7.0"
  }
}
